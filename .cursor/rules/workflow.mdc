---
description: Lite-web 与服务端同拍的发版与质量规则。
alwaysApply: true
---

# Lite-web 流程

跟 `Lite` 同一套口令：做功能 / 修 bug / 发快照 / 发稳定。UI 改动在本仓；接口在 Lite。文案走 `i18n:sync`，不要半边翻译。

本地开发禁止 `git push`，`main` 和 `Snapshot` 都要明确说了才能推。没明确表示：修改和验证一律只在本地。除非明确说了 `上传`、`发版`、`发快照`、`发稳定` 或 `上传发版`。只说合某一个 PR，就只做那一件。`暂不发版` = 停。push 本仓 `Snapshot` 会进服务端快照构建。

远端：https://github.com/nuomiiiii/Lite-web 。服务端仓：https://github.com/nuomiiiii/Lite 。未说口令禁止 push。

节点安装脚本和 Docker 一键命令走 `nuomiiiii/Lite-agent` 最新 `main` 脚本与 `ghcr.io/nuomiiiii/Lite-agent:latest`。Agent `2.3.0.0` 已原生适配 Lite，不要把一键安装锁死到某一个四段号。

验证：能在网页上走的模拟、验收和回归，必须在本地管理界面点，不要只打 API。网页才是用户直观使用的地方。同时检查文案、布局、交互和控件状态；发现问题记下来或当场改。

给用户的 Lite 二进制 / docker 包由 GitHub Actions 从源码构建：本仓只推源码和 `x.y.z` tag，`dist-release.zip` 由 `publish-dist-release.yml` 上传。不要把 `dist/` 提交进仓。`5273` 热更新不算已打进包。生产路径禁止预览/模拟数据。

分支：`main`、`Snapshot`。发快照：不改 `main`。远端没有 `Snapshot` 时，先从当前 `main` 新建 `Snapshot`，再在该分支上改；远端已有 `Snapshot` 时，直接在 `Snapshot` 上改。有 UI 改动时先推本仓 `Snapshot`，再推 Lite `Snapshot`（服务端 snapshot 构建固定 clone 本仓 Snapshot 分支）。**快照不要创建本仓 GitHub Release、tag 或 `dist-release.zip`。**

稳定 / 同版本再发：有 UI 改动时，先发本仓正式版，再发 Lite。把 `x.y.z` tag 移到本仓 `main`，更新 Release 说明（标题等于 `x.y.z`）。`dist-release.zip` 由 `publish-dist-release.yml` 构建并上传，不要在本地打包上传。不要恢复已删的 `follow-komari-release.yaml` / `build.yaml` 去自动镜像。快照显示名若出现在别处，必须是 `Snapshot-YYMMDDHHMM`（如 `Snapshot-2608162321`），不要「Lite 2.2.3 Snapshot」。

本仓工作流留 `i18n-sync.yml`、`generate-release-notes.yml` 和 `publish-dist-release.yml`。本地和 GitHub 都已删除、不要找回：`follow-komari-release.yaml`、`build.yaml`、`development.yaml`（SSH 部署）。没有 `dev`、`candidate/*`。

GitHub 文本 LF，禁止 AI/Cursor 标记。禁止把本地调试记录、验证记录、测试环境数据写进 commit、PR、Release 或仓库。后台热路径按弱机（GB5 单核约 600）想：不要一次预热三条重接口，不要把大数据塞进内存。
